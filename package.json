{
  "name": "svg-grid-editor",
  "displayName": "SVG Grid Editor",
  "description": "A grid-based SVG editor for VSCode",
  "version": "0.0.1",
  "engines": {
    "vscode": "^1.85.0"
  },
  "categories": [
    "Other"
  ],
  "activationEvents": [],
  "main": "./out/extension.js",
  "contributes": {
    "customEditors": [
      {
        "viewType": "svgGridEditor.editor",
        "displayName": "SVG Grid Editor",
        "selector": [
          {
            "filenamePattern": "*.svg"
          }
        ],
        "priority": "option"
      }
    ],
    "commands": [
      {
        "command": "svgGridEditor.openEditor",
        "title": "Open with SVG Grid Editor",
        "icon": "$(graph)"
      }
    ],
    "menus": {
      "explorer/context": [
        {
          "command": "svgGridEditor.openEditor",
          "when": "resourceExtname == .svg",
          "group": "navigation"
        }
      ],
      "editor/title": [
        {
          "command": "svgGridEditor.openEditor",
          "when": "resourceExtname == .svg",
          "group": "navigation",
          "icon": "$(graph)"
        }
      ]
    }
  },
  "scripts": {
    "vscode:prepublish": "npm run compile",
    "compile": "tsc -p ./",
    "watch": "tsc -watch -p ./",
    "pretest": "npm run compile && npm run lint",
    "lint": "eslint src --ext ts"
  },
  "devDependencies": {
    "@types/node": "^20.x",
    "@types/vscode": "^1.85.0",
    "@typescript-eslint/eslint-plugin": "^6.x",
    "@typescript-eslint/parser": "^6.x",
    "eslint": "^8.x",
    "typescript": "^5.3.0"
  }
}
